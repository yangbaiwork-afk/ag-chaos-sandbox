# Ag-Chaos Sandbox Design Specification (SPEC.md)

## 核心设计思想

Ag-Chaos Sandbox 旨在构建一个用于教学和验证农业具身智能（Embodied AI）安全性和鲁棒性的数字孪生实验平台。其核心哲学是：**在复杂的农业物理环境中，单纯依靠纯数据驱动的模型（如视觉、强化学习）或简单的动作映射是危险且不可靠的。必须引入基于农业领域先验知识（Ontology / 第一性原理）的安全拦截机制。**

我们通过向系统中注入各种“混沌（Chaos）”（例如：极端的环境变化、错误的视觉识别、不合理的动作请求），来测试系统在“有无规则约束”情况下的表现。

## 架构演进方向

1. **模块化关卡设计 (Levels)**:
   将不同的教学场景和混沌注入机制封装在独立的 Level 中。每个 Level 循序渐进地增加难度。从单株植物的简单空间拦截，到多株植物（成列、成片），再到引入风场、流体力学和复杂的植物生理学。

2. **左右/切换双屏对照模式 (Dual Twin System)**:
   为了直观展示规则的重要性，系统需要在同一时刻运行两个并行的物理孪生环境：
   - **实验组 (With Ontology)**: 受到严密的本体知识库（`ontology.json`）保护，拦截危险动作。
   - **对照组 (Without Ontology)**: 失去规则保护，忠实执行所有指令，展示由于缺乏约束导致的灾难性物理或生理后果。
   前端需要支持并排（Split-screen）和单屏切换（Toggle）模式，指令下发需同步到双环境。

3. **动态混沌注入 (Dynamic Chaos Injection)**:
   未来系统将提供教师/操作员面板，可以实时向物理引擎注入干扰信号（风切变、传感器置信度下降、极端温湿度）。

4. **物理与生理双重引擎**:
   不仅需要 MuJoCo 提供刚体、运动学、流体力学的基础仿真，还需要在代码层面实现基于第一性原理的“植物生理学”引擎（如水分胁迫、光合作用受阻等）。
