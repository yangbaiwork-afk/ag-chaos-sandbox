[project]
name = "ag-chaos-sandbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yangbaiwork-afk", email = "yangbai.work@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "asyncio>=4.0.0",
    "fastapi>=0.141.1",
    "mujoco>=3.11.0",
    "numpy>=2.5.2",
    "uvicorn>=0.30.0",
    "websockets>=12.0",
]

[project.scripts]
ag-chaos-sandbox = "ag_chaos_sandbox:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
